---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import SiteHeader from "../components/SiteHeader.astro";
import PortraitAnchor from "../components/PortraitAnchor.astro";
import ProjectProofCard from "../components/ProjectProofCard.astro";
import CoreArchitectureMap from "../components/CoreArchitectureMap.astro";
import { aiAgentLifecycleColumn, homepageActions, homepageContent, siteConfig, projects, socialLinks } from "../data/site";

const allEssays = (await getCollection("essays"))
  .sort((left, right) =>
    Number(right.data.flagship) - Number(left.data.flagship) ||
    right.data.publishDate.getTime() - left.data.publishDate.getTime()
  );

const essaysBySlug = new Map(allEssays.map((essay) => [essay.slug, essay]));
const readingPathEntries = homepageContent.readingPath
  .map((entry, index) => {
    const essay = essaysBySlug.get(entry.slug);
    return essay ? { ...entry, essay, index } : null;
  })
  .filter((entry): entry is NonNullable<typeof entry> => Boolean(entry));

const agenticDeliveryFlowRows = [
  homepageContent.coreThesis.agenticDeliveryFlow.slice(0, 4),
  homepageContent.coreThesis.agenticDeliveryFlow.slice(4)
];

const writingTracks = [
  { name: "Protocol Engineering", desc: "Lifecycle vocabulary, handoff semantics, and delivery grammar for agent systems." },
  { name: "Reliable Agent Delivery", desc: "How agent systems complete real work — plannable, verifiable, and auditable." },
  { name: "Agent Governance & Evidence", desc: "Oversight structures, compliance surfaces, and accountability frameworks for autonomous work." },
  { name: "AI Programming & Delivery Systems", desc: "Runtime architecture, state management, and operating constraints for protocol-aware agents." },
  { name: "Agent Stack Commentary", desc: "Analysis and perspective on the evolving agent infrastructure landscape." }
];
---

<BaseLayout
  title={siteConfig.title}
  description={siteConfig.description}
  shareTitle="Who Governs the Lifecycle?"
  shareDescription="AI agents are learning to act, but reliable delivery still needs lifecycle accountability. Jearon Wong writes and builds around Agentic Delivery."
  ogImagePath="/social/jearonwong-og.png"
  ogImageAlt="Who governs the lifecycle? AI agents can act. Reliable delivery still needs accountability."
>
  <!-- 1. Problem + Concept + Author Anchor (Hero) -->
  <div class="zone-a registry-grid hero-wrapper" style="border-bottom: 1px solid var(--border-registry);">
    <SiteHeader />
    
    <section class="shell hero-section">
      <div class="split-layout">
        <div class="hero-content">
          <div class="hero-technical-hook registry-annotation">
            {homepageContent.hero.technicalHookLines.map((line) => (
              <span>{line}</span>
            ))}
          </div>
          <h1 class="hero-nameplate">
            {homepageContent.hero.hookLines.map((line) => (
              <span>{line}</span>
            ))}
          </h1>
          <p class="hero-thesis">{homepageContent.hero.concept}</p>
          <div class="hero-author-anchor">
            <span class="hero-author-name">{homepageContent.hero.authorAnchor}</span>
            <span class="hero-role-line">{homepageContent.hero.role}</span>
          </div>
          <p class="hero-support">{homepageContent.hero.support}</p>
          
          <div class="hero-ctas">
            <a href={homepageActions.thesis.href} class="button button-primary">{homepageActions.thesis.label}</a>
            <a href={homepageActions.projects.href} class="button button-outline" style="border: 1px solid var(--border-registry); background: rgba(255,255,255,0.05);">{homepageActions.projects.label}</a>
          </div>
        </div>
        <div class="hero-visual">
          <PortraitAnchor />
        </div>
      </div>
    </section>
  </div>

  <!-- 2. Stack Architecture -->
  <div class="zone-b page-body page-body--stack">
    <CoreArchitectureMap />
  </div>

  <!-- 3. Core Thesis -->
  <section class="zone-a registry-grid core-thesis-band">
    <div class="shell core-thesis-layout">
      <div class="core-thesis-copy">
        <div class="registry-annotation core-thesis-eyebrow">
          <span class="registry-key">{homepageContent.coreThesis.eyebrow}</span>
        </div>
        <h2>{homepageContent.coreThesis.title}</h2>
        <p><strong>Most agent systems optimize execution:</strong> {homepageContent.coreThesis.execution.replace("Most agent systems optimize execution: ", "")}</p>
        <p><strong>My work starts from delivery:</strong> {homepageContent.coreThesis.delivery.replace("My work starts from delivery: ", "")}</p>
      </div>

      <div class="thesis-compare registry-chassis" aria-label="Task execution versus Agentic Delivery">
        <div class="compare-track">
          <span class="compare-label">{homepageContent.coreThesis.taskAgentLabel}</span>
          <div class="compare-flow">
            {homepageContent.coreThesis.taskAgentFlow.map((step, index) => (
              <>
                <span class="compare-step">{step}</span>
                {index < homepageContent.coreThesis.taskAgentFlow.length - 1 && <span class="compare-arrow" aria-hidden="true">-&gt;</span>}
              </>
            ))}
          </div>
        </div>

        <div class="compare-track compare-track--delivery">
          <span class="compare-label">{homepageContent.coreThesis.agenticDeliveryLabel}</span>
          <div class="compare-flow compare-flow--delivery">
            {agenticDeliveryFlowRows.map((row, rowIndex) => (
              <div class:list={["compare-flow-row", rowIndex > 0 && "compare-flow-row--continuation"]}>
                {row.map((step, index) => (
                  <>
                    <span class="compare-step">{step}</span>
                    {index < row.length - 1 && <span class="compare-arrow" aria-hidden="true">-&gt;</span>}
                  </>
                ))}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="zone-b page-body">
    <!-- 4. AI Agent Lifecycle Category Bridge -->
    <section class="shell section-rhythm lifecycle-category-bridge">
      <div class="registry-chassis lifecycle-bridge-panel">
        <div class="registry-annotation" style="margin-bottom: 0.85rem;">
          <span class="registry-key">CATEGORY_ENTRY</span>: <span class="registry-value">AI AGENT LIFECYCLE</span>
        </div>
        <div class="lifecycle-bridge-grid">
          <div>
            <h2>Start from AI Agent Lifecycle.</h2>
            <p class="section-desc">{aiAgentLifecycleColumn.relationToAgenticDelivery}</p>
          </div>
          <div class="lifecycle-bridge-copy">
            <p>{aiAgentLifecycleColumn.categoryBridge}</p>
            <div class="lifecycle-bridge-links">
              <a href={homepageActions.lifecycle.href} class="section-link">{homepageActions.lifecycle.label}</a>
              <a href={homepageActions.essays.href} class="section-link">{homepageActions.essays.label}</a>
              <a href={homepageActions.proof.href} class="section-link">{homepageActions.proof.label}</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 5. Proof Surfaces -->
    <section id="proof" class="shell section-rhythm">
      <div class="section-header">
        <h2>Proof Surfaces</h2>
        <p class="section-desc">These projects express Agentic Delivery through a concrete reference path, without closing the category.</p>
        <a href={homepageActions.proof.href} class="section-link">{homepageActions.proof.label}</a>
      </div>
      <div class="card-grid">
        {projects.map(project => (
          <ProjectProofCard
            {...project}
            href={`/projects/${project.slug}/`}
            compact
          />
        ))}
      </div>
    </section>

    <!-- 6. Reading Path -->
    {readingPathEntries.length > 0 && (
      <section class="shell section-rhythm" style="padding-top: 2rem;">
      <div class="section-header">
        <h2>Reading Path</h2>
        <p class="section-desc">The essays develop the public argument after the Lifecycle mainline is clear.</p>
        <a href={homepageActions.essays.href} class="section-link">{homepageActions.essays.label}</a>
      </div>
        <div class="reading-path-list">
          {readingPathEntries.map((entry) => (
            <article class="reading-path-card">
              <span class="reading-path-index">{String(entry.index + 1).padStart(2, "0")}</span>
              <div class="reading-path-content">
                <h3><a href={`/essays/${entry.essay.slug}/`}>{entry.essay.data.title}</a></h3>
                <p>{entry.note}</p>
                <a href={`/essays/${entry.essay.slug}/`} class="section-link">Read essay</a>
              </div>
            </article>
          ))}
        </div>
      </section>
    )}

    <!-- 7. Writing Tracks -->
    <section class="shell section-rhythm" style="padding-top: 2rem;">
      <div class="section-header">
        <h2>Writing tracks.</h2>
        <p class="section-desc">Supporting lines of inquiry across protocols, governance, runtime, delivery, and product.</p>
      </div>
      <div class="tracks-grid">
        {writingTracks.map(track => (
          <div class="track-item">
            <h4 class="track-name">{track.name}</h4>
            <p class="track-desc">{track.desc}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- 8. Contact / Signal -->
    <section class="shell section-rhythm quiet-close" style="padding-top: 2rem;">
      <div class="close-content">
        <h2>Get in touch.</h2>
        <p class="section-desc">For industry dialogue, advisory, collaboration, or media.</p>
        <div class="close-actions">
          <a href={`mailto:${siteConfig.author.email}`} class="button button-primary" style="background: #111; color: #fff;">{siteConfig.author.email}</a>
          <div class="close-social">
            {socialLinks.map(link => (
              <a href={link.href} target="_blank" rel="noopener noreferrer" class="social-pill">{link.platform}</a>
            ))}
          </div>
        </div>
      </div>
    </section>

  </div>
</BaseLayout>

<style>
  /* ── Hero ── */
  .hero-wrapper {
    position: relative;
    overflow: hidden;
    min-height: 100svh;
  }
  .hero-section {
    padding-top: clamp(4.75rem, 6.1vw, 5.5rem);
    padding-bottom: clamp(5rem, 6.4vw, 6rem);
  }
  .hero-technical-hook {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.24rem;
    margin-bottom: 1.35rem;
    color: var(--accent-signal);
    font-size: 0.75rem;
    line-height: 1.55;
    letter-spacing: 0.05em;
    max-width: min(100%, 46ch);
  }
  .hero-technical-hook span {
    display: block;
    max-width: 100%;
    white-space: normal;
    overflow-wrap: break-word;
  }
  .hero-nameplate {
    font-size: clamp(3.25rem, 4.2vw, 3.55rem);
    font-weight: 780;
    letter-spacing: -0.03em;
    line-height: 1.045;
    margin: 0 0 1.45rem;
    color: var(--text-registry-primary);
    max-width: 17ch;
    text-wrap: wrap;
    overflow-wrap: normal;
  }
  .hero-nameplate span {
    display: block;
  }
  .hero-thesis {
    font-size: clamp(1.42rem, 1.9vw, 1.6rem);
    font-weight: 630;
    letter-spacing: -0.01em;
    line-height: 1.34;
    margin: 0 0 1.9rem;
    color: rgba(241, 245, 249, 0.88);
    max-width: 39rem;
  }
  .hero-author-anchor {
    display: grid;
    gap: 0.28rem;
    margin-bottom: 1.25rem;
  }
  .hero-author-name {
    font-weight: 740;
    color: var(--text-registry-primary);
    letter-spacing: -0.01em;
    font-size: 1.08rem;
  }
  .hero-role-line {
    font-family: var(--font-mono);
    font-size: 0.74rem;
    text-transform: uppercase;
    letter-spacing: 0.065em;
    color: rgba(148, 163, 184, 0.78);
    font-weight: 600;
    line-height: 1.45;
    max-width: 44ch;
  }
  .hero-support {
    max-width: 35rem;
    color: rgba(148, 163, 184, 0.84);
    font-size: clamp(1.03rem, 1.25vw, 1.06rem);
    line-height: 1.64;
    font-weight: 400;
    margin-bottom: 1.9rem;
  }
  .hero-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  /* ── Sections ── */
  .section-header h2 {
    font-size: clamp(1.8rem, 3.2vw, 2.35rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    font-weight: 800;
    margin-bottom: 0.6rem;
  }
  .section-desc {
    font-size: 1rem;
    color: var(--current-muted);
    line-height: 1.6;
    max-width: min(100%, 58rem);
    margin-bottom: 0.25rem;
  }
  .section-link {
    display: inline-flex;
    align-items: center;
    margin-top: 0.9rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent-signal);
    text-decoration: none;
  }
  .section-link:hover {
    color: var(--current-text);
  }
  .section-header {
    margin-bottom: 3.5rem;
  }

  .lifecycle-category-bridge {
    padding-top: 7rem;
    padding-bottom: 2rem;
  }

  .lifecycle-bridge-panel {
    padding: 2rem;
    border-left: 2px solid var(--accent-proof);
  }

  .lifecycle-bridge-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 2.5rem;
    align-items: start;
  }

  .lifecycle-bridge-grid h2 {
    margin: 0 0 0.75rem;
    font-size: clamp(1.6rem, 3vw, 2.25rem);
    line-height: 1.08;
    letter-spacing: -0.02em;
    color: var(--current-text);
  }

  .lifecycle-bridge-copy p {
    margin: 0;
    max-width: 54rem;
    color: var(--current-muted);
    font-size: 1rem;
    line-height: 1.65;
  }

  .lifecycle-bridge-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0 1rem;
  }

  /* ── Core Thesis ── */
  .core-thesis-band {
    padding: 6rem 0;
    border-top: 1px solid var(--border-registry);
    border-bottom: 1px solid var(--border-registry);
  }

  .core-thesis-layout {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 4rem;
    align-items: center;
  }

  .core-thesis-eyebrow {
    margin-bottom: 1rem;
  }

  .core-thesis-copy h2 {
    font-size: clamp(2rem, 4vw, 2.8rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin: 0 0 1.35rem;
    font-weight: 800;
  }

  .core-thesis-copy p {
    max-width: 42rem;
    margin: 0;
    color: var(--text-registry-muted);
    font-size: 1rem;
    line-height: 1.7;
  }

  .core-thesis-copy p + p {
    margin-top: 1rem;
  }

  .core-thesis-copy strong {
    color: var(--text-registry-primary);
  }

  .thesis-compare {
    padding: 1.65rem 1.85rem;
    background-color: rgba(10, 10, 10, 0.54);
  }

  .compare-track {
    display: grid;
    grid-template-columns: 9.5rem minmax(0, 1fr);
    gap: 1.25rem;
    align-items: start;
  }

  .compare-track + .compare-track {
    margin-top: 1.25rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--border-registry);
  }

  .compare-label {
    display: block;
    margin-top: 0.45rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent-signal);
  }

  .compare-track--delivery .compare-label {
    color: var(--accent-proof);
  }

  .compare-flow {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
    min-width: 0;
  }

  .compare-flow--delivery {
    display: grid;
    gap: 0.62rem;
  }

  .compare-flow-row {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 0.5rem;
    min-width: 0;
  }

  .compare-flow-row--continuation {
    padding-left: 0;
  }

  .compare-step {
    padding: 0.42rem 0.62rem;
    border: 1px solid var(--border-registry);
    background: rgba(255, 255, 255, 0.035);
    color: var(--text-registry-primary);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.03em;
    white-space: nowrap;
    text-align: center;
  }

  .compare-arrow {
    color: var(--text-registry-muted);
    font-family: var(--font-mono);
    font-size: 0.68rem;
  }

  /* ── Card Grid ── */
  .card-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.1rem;
    align-items: stretch;
  }
  .card-grid :global(.evidence-record) {
    height: 100%;
  }

  /* ── Reading Path ── */
  .reading-path-list {
    display: grid;
    gap: 1rem;
  }

  .reading-path-card {
    display: grid;
    grid-template-columns: 4.5rem minmax(0, 1fr);
    gap: 1.25rem;
    background: var(--current-surface);
    border: 1px solid var(--current-border);
    border-left: 4px solid var(--accent-signal);
    border-radius: var(--radius-module);
    padding: 1.6rem 1.75rem;
  }

  .reading-path-index {
    font-family: var(--font-mono);
    font-size: 0.76rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    color: var(--accent-signal);
  }

  .reading-path-content h3 {
    font-size: 1.5rem;
    font-weight: 800;
    margin-bottom: 0.75rem;
    letter-spacing: -0.02em;
    line-height: 1.18;
  }

  .reading-path-content h3 a {
    color: inherit;
    text-decoration: none;
  }

  .reading-path-content h3 a:hover {
    color: var(--accent-signal);
  }

  .reading-path-content p {
    color: var(--current-muted);
    line-height: 1.6;
    max-width: 60ch;
    margin: 0;
  }

  /* ── Writing Tracks ── */
  .tracks-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 0;
  }
  .track-item {
    padding: 1.5rem 1.5rem 1.5rem 0;
    border-bottom: 1px solid var(--current-line);
  }
  .track-name {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--accent-signal);
    margin-bottom: 0.5rem;
  }
  .track-desc {
    font-size: 0.9rem;
    color: var(--current-muted);
    line-height: 1.5;
    margin: 0;
  }

  /* ── Quiet Close ── */
  .quiet-close {
    padding-bottom: 2rem;
  }
  .close-content h2 {
    font-size: 1.8rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
  }
  .close-actions {
    margin-top: 1.5rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: center;
  }
  .close-social {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .social-pill {
    display: inline-flex;
    align-items: center;
    padding: 0.45rem 0.85rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--current-muted);
    border: 1px solid var(--current-border);
    border-radius: 2px;
    text-decoration: none;
    transition: border-color 0.2s ease, color 0.2s ease;
  }
  .social-pill:hover {
    border-color: var(--accent-signal);
    color: var(--accent-signal);
  }

  /* ── Layout ── */
  .split-layout {
    display: grid;
    grid-template-columns: minmax(0, 0.6fr) minmax(400px, 0.4fr);
    gap: clamp(3.5rem, 4vw, 4rem);
    align-items: center;
  }
  .hero-visual {
    justify-self: end;
    align-self: center;
    width: min(100%, 27.5rem);
    padding-top: 0;
  }

  /* ── Responsive ── */
  @media (max-width: 1024px) {
    .split-layout {
      gap: 2.75rem;
      grid-template-columns: 1fr;
      align-items: start;
    }
    .hero-visual {
      justify-self: start;
      width: min(100%, 23.75rem);
      padding-top: 0;
    }
  }

  @media (max-width: 980px) {
    .card-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1.25rem;
    }
  }

  @media (max-width: 900px) {
    .split-layout {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .hero-section {
      padding-top: 4rem;
      padding-bottom: 4.25rem;
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      padding-top: 4rem;
      padding-bottom: 4rem;
    }
    .hero-nameplate {
      font-size: clamp(1.9rem, 7vw, 2.5rem);
      line-height: 1.055;
      max-width: 100%;
      letter-spacing: -0.028em;
    }
    .hero-technical-hook {
      margin-bottom: 1.2rem;
    }
    .hero-thesis {
      font-size: clamp(1.08rem, 4.5vw, 1.25rem);
      line-height: 1.36;
      max-width: 31ch;
      margin-bottom: 1.75rem;
    }
    .hero-role-line {
      max-width: 31ch;
    }
    .hero-support {
      font-size: 1rem;
      max-width: 34ch;
      margin-bottom: 1.65rem;
    }
    .hero-visual {
      width: min(100%, 21.25rem);
    }
    .hero-ctas {
      display: grid;
      grid-template-columns: 1fr;
      gap: 0.55rem;
      max-width: 100%;
    }
    .hero-ctas .button {
      min-height: 2.65rem;
      padding-inline: 0.7rem;
      font-size: 0.82rem;
      text-align: center;
      white-space: normal;
    }
    .section-header {
      margin-bottom: 2.5rem;
    }
    .lifecycle-category-bridge {
      padding-top: 4.5rem;
      padding-bottom: 1.5rem;
    }
    .lifecycle-bridge-panel {
      padding: 1.5rem;
    }
    .lifecycle-bridge-grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }
    .core-thesis-band {
      padding: 4.5rem 0;
    }
    .core-thesis-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .thesis-compare {
      padding: 1.35rem;
    }
    .compare-track {
      grid-template-columns: 1fr;
      gap: 0.8rem;
    }
    .compare-label {
      margin-top: 0;
      margin-bottom: 0;
    }
    .tracks-grid {
      grid-template-columns: 1fr;
    }
    .reading-path-card {
      padding: 1.75rem;
    }
  }

  @media (max-width: 620px) {
    .card-grid {
      grid-template-columns: 1fr;
    }
    .compare-flow--delivery {
      gap: 0.5rem;
    }
    .compare-track--delivery .compare-flow-row {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.5rem;
    }
    .compare-track--delivery .compare-arrow {
      display: none;
    }
    .compare-track--delivery .compare-step {
      width: 100%;
      min-height: 2rem;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .compare-flow-row--continuation .compare-step:last-child {
      grid-column: 1 / -1;
    }
    .reading-path-card {
      grid-template-columns: 1fr;
      gap: 0.8rem;
    }
  }
</style>
