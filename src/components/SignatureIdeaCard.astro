---
interface Props {
  name: string;
  description: string;
  prompt: string;
}

const { name, description, prompt } = Astro.props;
---

<article class="registry-chassis system-pillar">
  <div class="pillar-header">
    <div class="registry-annotation">
      <span class="registry-key">PILLAR_ID</span>: <span class="registry-value">{name.toUpperCase().replace(/\s/g, '_')}</span>
    </div>
  </div>
  
  <div class="pillar-content">
    <div class="registry-annotation" style="margin-bottom: 0.5rem;">
      <span class="registry-key">PILLAR_NAME</span>
    </div>
    <h3 class="pillar-title">{name}</h3>
    <p class="pillar-desc">{description}</p>
    
    <div class="focus-block">
      <div class="registry-annotation" style="margin-bottom: 0.75rem;">
        <span class="registry-key">SPECIFICATION_FOCUS</span>
      </div>
      <p class="focus-text">{prompt}</p>
    </div>
  </div>
</article>

<style>
  .system-pillar {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    background-color: var(--current-surface);
    border: 1px solid var(--current-border);
  }
  .pillar-header {
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--current-line);
  }
  .pillar-title {
    font-size: 1.5rem;
    font-weight: 850;
    margin: 0 0 1rem 0;
    letter-spacing: -0.03em;
    color: var(--current-text);
  }
  .pillar-desc {
    font-size: 1rem;
    color: var(--current-muted);
    line-height: 1.55;
    margin-bottom: 1.5rem;
  }
  .focus-block {
    padding: 1.25rem;
    border: 1px solid var(--border-registry);
    background-color: rgba(255, 255, 255, 0.02);
  }
  .focus-text {
    font-size: 0.9rem;
    font-weight: 500;
    line-height: 1.5;
    margin: 0;
    color: var(--current-text);
    opacity: 0.8;
  }

  @media (max-width: 768px) {
    .system-pillar {
      padding: 1.75rem;
      gap: 1.1rem;
    }

    .pillar-title {
      font-size: 1.32rem;
      margin-bottom: 0.8rem;
    }

    .pillar-desc {
      font-size: 0.96rem;
      margin-bottom: 1.2rem;
    }

    .focus-block {
      padding: 1rem;
    }

    .focus-text {
      font-size: 0.86rem;
    }
  }
</style>
